metric: polygraphscore
classifier: auto  # auto = TabPFN if installed, otherwise standardized logistic regression fallback
reference_split: test
num_splits: 3
cv_folds: 4
skip_orbits: false
include_attribute_descriptor: true
interpretation: PGS is a held-out JS-distance lower-bound estimate in [0,1]; lower is better.
