# Optional dependencies for external model wrappers.
# Install only the subset required by the wrappers you plan to run.


# GraphGUIDE wrapper dependencies, in addition to the uploaded upstream GraphGUIDE repo:
# The benchmark wrapper avoids upstream Sacred training entry points but uses
# GraphGUIDE's PyG graph conversion, GAT/GIN edge predictor, Bernoulli diffusers,
# and sampling utilities.
torch-geometric
tqdm
# sacred is needed only if you run upstream src/model/train_model.py directly.
# sacred

# DiGress core wrapper dependencies, in addition to the upstream DiGress repo:
omegaconf
hydra-core
pytorch-lightning
wandb
torch-geometric
# torch should be installed following the CUDA/CPU instructions for your machine.
# graph-tool is needed only for upstream DiGress SPECTRE sampling metrics; this
# benchmark wrapper can train/sample without it because benchmark metrics are
# computed separately.

# Molecular DiGress datasets additionally require rdkit.
# rdkit

# ConStruct wrapper dependencies, in addition to the uploaded upstream ConStruct repo:
# The benchmark wrapper avoids upstream PyG datamodules, Lightning trainer,
# graph-tool/ORCA sampling metrics, and visualization paths. It still uses the
# upstream dense Transformer/noise/feature/projector modules. Full upstream
# scripts require pytorch-lightning, torchmetrics, torch-geometric, graph-tool,
# RDKit, imageio, and wandb depending on the selected path.
networkx
numpy
# scipy is used indirectly by NetworkX for some spectral/linear-algebra routines.
scipy
# pytorch-lightning
# torchmetrics
# torch-geometric
# graph-tool
# rdkit
# imageio
# wandb

# EDP-GNN wrapper dependencies, in addition to the uploaded upstream EDP-GNN repo:
scipy
matplotlib
PyYAML
# The benchmark wrapper provides an EasyDict fallback and does not import the
# upstream executable scripts, but easydict may still be needed if you run those
# upstream scripts directly.
# easydict
# pyemd is needed only for the original upstream evaluation scripts; benchmark
# metrics are handled by this project.
# pyemd

# DisCo wrapper dependencies, in addition to the uploaded upstream DisCo repo:
# The benchmark wrapper avoids upstream dataset/evaluation imports, but full
# upstream scripts still require torch-geometric, torchmetrics, pyemd, graph-tool,
# and RDKit depending on the dataset/evaluation path.
tqdm
# torch-geometric
# torchmetrics
# pyemd
# graph-tool
# rdkit

# GruM wrapper dependencies, in addition to the uploaded upstream GruM repo:
# The benchmark wrapper avoids upstream main/trainer/sampler scripts and does not
# require graph-tool, RDKit, pyemd, fcd-torch, or tensorboard for benchmark
# generic-graph training/sampling. Full upstream scripts may require the full
# requirements.txt from the GruM repository.
tqdm
PyYAML
scipy
# easydict is optional for the benchmark wrapper because it provides a fallback.
# easydict
# graph-tool, rdkit, pyemd, fcd-torch, tensorboard are needed only for upstream
# evaluation/molecule workflows.
